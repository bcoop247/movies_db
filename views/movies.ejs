<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h2>Movies</h2>
  <!-- We are selecting the data to display from within "/routes/movies.js" -->
  <ul>
    <% movies.forEach(movie => { %>
        <li>Movie: <a href="movies/<%=movie.id%>"><%= movie.movie %></a></li>
    <% }) %>
  </ul>

  
</body>
</html>